fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-dotenv==1.0.1
httpx==0.27.2

# LLM client (Groq, OpenAI-compatible API)
openai==1.51.0

# RAG stack — chromadb ships its own ONNX-runtime MiniLM by default
# (much lighter than sentence-transformers/PyTorch — fits in 512 MB Render free tier).
chromadb==0.5.5

# C++ addon build
pybind11==2.13.6
